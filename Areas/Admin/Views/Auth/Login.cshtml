@{
    // Título de la página y layout principal
    ViewData["Title"] = "Acceso Administrador";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Vista de login para el usuario administrador -->
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6 col-lg-4">

            <!-- Tarjeta principal del formulario -->
            <div class="card mt-5 shadow-sm">
                <div class="card-header text-center bg-primary text-white">
                    <h4>🔐 Acceso Administrador</h4>
                </div>

                <div class="card-body">
                    <!-- Muestra mensaje de error si las credenciales son incorrectas -->
                    @if (!string.IsNullOrEmpty(ViewBag.Error))
                    {
                        <div class="alert alert-danger" role="alert">
                            @ViewBag.Error
                        </div>
                    }

                    <!-- Formulario que envía los datos al AuthController (método Login) -->
                    <form method="post" asp-action="Login" asp-controller="Auth" asp-area="Admin">

                        <!-- Campo de usuario -->
                        <div class="mb-3">
                            <label for="username" class="form-label">Usuario:</label>
                            <input type="text" class="form-control" id="username" name="username" required>
                        </div>

                        <!-- Campo de contraseña -->
                        <div class="mb-3">
                            <label for="password" class="form-label">Contraseña:</label>
                            <input type="password" class="form-control" id="password" name="password" required>
                        </div>

                        <!-- Botón de envío -->
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">Iniciar Sesión</button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Botón para volver al sitio público -->
            <div class="text-center mt-3">
                <!-- Botón de retorno al inicio del sitio -->
                <a href="@Url.Action("Index", "Home", new { area = "" })" class="btn btn-outline-secondary">
                    ← Volver al sitio principal
                </a>
            </div>
        </div>
    </div>
</div>
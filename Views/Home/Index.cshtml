@{
    ViewData["Title"] = "Refugio de Animales";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!-- Sección principal de inicio con fondo de huellas -->
<section class="inicio">
    <div class="contenido">
        <!-- Texto a la izquierda -->
        <div class="texto">
            <h1 class="titulo">🐾 Animalia</h1>
            <p>
                <strong>
                    En nuestro refugio creemos que cada mascota merece un hogar lleno de cariño.
                    Rescatamos, cuidamos y preparamos a perros y gatos para que puedan tener una segunda oportunidad.
                    Adoptar no solo cambia su vida, también transforma la tuya.
                    Explorá nuestras secciones y conocé a los peludos que están esperando una familia.
                    ¡Tu nuevo mejor amigo te está buscando!
                </strong>
            </p>
            <p class="instagram">🐾 <strong>Animalia</strong></p>
        </div>

        <!-- Imagen a la derecha -->
        <div class="imagen">
            <img src="/images/NewFolder/mascotas inicio.png" alt="Mascotas felices">
        </div>
    </div>
</section>

<!-- Estilos -->
<style>
    /* Fondo y tipografía */
    body {
        background-color: #ffffff;
        background-image: url('/images/NewFolder/fondo huellas.jpg');
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        font-family: 'Life Savers', cursive;
        color: #3a2f3c;
    }

    /* Sección principal */
    .inicio {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 85vh;
        padding: 40px 60px;
    }

    /* Contenedor */
    .contenido {
        display: flex;
        align-items: center;
        justify-content: space-between;
        max-width: 1200px;
        width: 100%;
        gap: 40px;
    }

    /* Texto a la izquierda */
    .texto {
        flex: 1;
        font-size: 1.1rem;
        line-height: 1.7;
        text-align: left;
    }

    .titulo {
        color: #7a3ca3;
        font-weight: 700;
        font-size: 3rem;
        margin-bottom: 25px;
    }

    .instagram {
        margin-top: 30px;
        color: #5c326c;
        font-weight: 600;
        font-size: 1.3rem;
    }

    /* Imagen a la derecha */
    .imagen {
        flex: 1;
        text-align: right;
    }

        .imagen img {
            width: 90%;
            max-width: 500px;
            filter: drop-shadow(0 5px 10px rgba(0, 0, 0, 0.15));
        }

    /* Botones y menú */
    .menu-superior a {
        background-color: #efe8e2;
        color: #4a305a;
        font-weight: bold;
        text-decoration: none;
        margin: 0 10px;
        padding: 10px 25px;
        border-radius: 20px;
        transition: all 0.3s ease;
    }

        .menu-superior a:hover {
            background-color: #7a3ca3;
            color: #fff;
        }

    .btn-admin {
        background-color: #7a3ca3;
        color: #fff;
        font-weight: bold;
        padding: 10px 25px;
        border-radius: 30px;
        text-decoration: none;
        margin-left: 20px;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.2);
        transition: all 0.3s ease-in-out;
    }

        .btn-admin:hover {
            background-color: #9c54c3;
            transform: scale(1.05);
        }

    /* Responsivo */
   (max-width: 768px) {
        .contenido

    {
        flex-direction: column;
        text-align: center;
    }

    .texto {
        padding-right: 0;
    }

    .imagen {
        margin-top: 25px;
        text-align: center;
    }

        .imagen img {
            width: 100%;
            max-width: 400px;
        }

    .titulo {
        font-size: 2.2rem;
    }

    }
</style>
